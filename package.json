{
  "name": "live-chat-support",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && bun run dev",
    "dev:backend": "cd apps/backend && bun run dev"
  }
}